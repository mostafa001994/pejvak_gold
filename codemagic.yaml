workflows:
  flutter-release:
    name: Build Flutter App for iOS and Android
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Install dependencies
        script: |
          flutter pub get
      - name: Build Android APK (arm64 only)
        script: |
          flutter build apk --release --target-platform android-arm64
      - name: Build iOS IPA
        script: |
          flutter build ios --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - goldpejvak@gmail.com